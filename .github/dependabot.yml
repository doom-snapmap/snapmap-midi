# Two dependency surfaces here: the SHA-pinned GitHub Actions, and pip -- the MIDI parser and
# desktop UI host are runtime dependencies, so they are worth watching alongside the workflows.
#
# When an action publishes a new release Dependabot opens a PR updating the pinned commit SHA
# (and its version comment), so the pins stay both immutable and up to date.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
    groups:
      actions:
        patterns:
          - "*"

  - package-ecosystem: pip
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: deps
